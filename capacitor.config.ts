import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.optirestmozo.app',
  appName: 'opti-rest.mozo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
